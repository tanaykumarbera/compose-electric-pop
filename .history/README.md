# `.history/`

Frozen-in-time artifacts preserved for future reference. **Not source of truth** — current state lives in code, CLAUDE.md, AGENTS.md, and the GitHub Pages reference site.

Each entry is dated. Files here are intentionally not updated; if a fact has changed, update the live source instead.
